# Re:World Architecture

Re:World uses a multi-agent architecture.

User
→ FastAPI
→ Router Agent
→ Character / Narrative / Story Analyst
→ Shared Tools
→ World State + Retrieval
→ Consistency Agent
→ Response

Canonical story state is immutable.
Alternate timelines are isolated branches.